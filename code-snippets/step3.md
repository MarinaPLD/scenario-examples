# Step 3: Download and Set Up BunkerWeb

You should now be able to install NGINX 1.26.2 using the following commands:

```bash
sudo apt update && \
sudo apt install -y nginx=1.26.2-1~$(lsb_release -cs)
